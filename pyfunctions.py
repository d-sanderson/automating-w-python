from random import * # this line imports everything from the random python module
# this allows you to type randint(x, y) in place of random.randint(x, y)
import sys
randomNum = randint(1, 100)
print(randomNum)
print('Hello')
sys.exit() # program will exit before line 8 will execute
print('GoodBye')
